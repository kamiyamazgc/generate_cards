name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: python -m pip install -r requirements.txt -r requirements-dev.txt --no-cache-dir
      - run: pytest -q
